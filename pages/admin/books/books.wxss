/* pages/admin/books/books.wxss */
.filter-bar {
  display: flex; gap: 16rpx; margin-bottom: 20rpx;
}
.filter-tab {
  padding: 12rpx 28rpx; border-radius: 24rpx; font-size: 26rpx;
  background: #fff; color: #666; box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.04);
}
.filter-tab.active { background: #4A90E2; color: #fff; }
.admin-book-card {
  background: #fff; border-radius: 16rpx; padding: 20rpx;
  margin-bottom: 16rpx; box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04);
}
.book-row { display: flex; align-items: center; }
.book-thumb { width: 100rpx; height: 130rpx; border-radius: 8rpx; margin-right: 16rpx; background: #f5f5f5; }
.book-info { flex: 1; }
.book-title { font-size: 28rpx; font-weight: 500; display: block; max-width: 380rpx; }
.status-dot { width: 16rpx; height: 16rpx; border-radius: 50%; flex-shrink: 0; }
.admin-actions { display: flex; justify-content: flex-end; gap: 12rpx; margin-top: 16rpx; padding-top: 16rpx; border-top: 1rpx solid #f5f5f5; }
.action-btn { padding: 8rpx 24rpx; font-size: 24rpx; border-radius: 20rpx; background: #f5f5f5; color: #666; border: none; line-height: 1.5; }
.action-btn::after { border: none; }
.btn-primary-sm { background: #4A90E2; color: #fff; }
.btn-danger-sm { background: #fff; color: #F56C6C; border: 1rpx solid #F56C6C; }
.loading-state { text-align: center; padding: 80rpx 0; }
.empty-state { text-align: center; padding: 80rpx 0; }
