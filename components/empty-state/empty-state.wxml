<view class="empty-state-wrap">
  <text class="empty-icon">{{icon}}</text>
  <text class="empty-text">{{text}}</text>
  <text wx:if="{{subText}}" class="empty-sub">{{subText}}</text>
  <button wx:if="{{btnText}}" class="empty-btn btn-primary" bindtap="onAction">{{btnText}}</button>
</view>
